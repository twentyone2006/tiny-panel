# Tiny Panel

## 项目简介
Tiny Panel是一个轻量级的Linux服务器管理面板，旨在提供简单易用的服务器管理功能，适合个人和小型团队使用。

## 功能特性
- 🔐 **用户认证系统**：支持多用户、权限管理
- 🖥️ **服务器监控**：实时监控CPU、内存、磁盘使用情况
- 📊 **系统统计**：查看系统资源使用历史图表
- 📁 **文件管理**：上传、下载、编辑、删除文件
- 🌐 **网络监控**：查看网络连接和流量统计
- 📋 **进程管理**：查看和管理系统进程
- 📝 **系统日志**：查看系统日志文件
- 🛠️ **快速工具**：提供常用系统管理工具

## 系统要求
- Linux操作系统（CentOS 7+、Ubuntu 18.04+、Debian 10+）
- Python 3.7+
- pip 20.0+
- 1GB以上内存
- 10GB以上磁盘空间
- 可访问的服务器IP和开放的8888端口

## 快速安装（推荐）
使用一键安装脚本，只需一行命令即可完成安装：

```bash
curl -sSL http://www.tiny-lab.cn/dl/install.sh | bash
```

### 一键安装脚本功能
- ✅ 自动检查操作系统和Python版本
- ✅ 安装所有必要的系统依赖
- ✅ 创建独立的Python虚拟环境
- ✅ 下载最新代码并安装Python依赖
- ✅ 初始化数据库和默认账号
- ✅ 创建系统服务，支持开机自启
- ✅ 配置日志和数据目录

## 手动安装

### 1. 克隆代码库
```bash
git clone https://github.com/tiny-panel/tiny-panel.git
cd tiny-panel
```

### 2. 安装系统依赖

**CentOS/RHEL:**
```bash
yum update -y
yum install -y gcc python3-devel python3-pip mariadb-devel openssl-devel git
```

**Ubuntu/Debian:**
```bash
apt-get update -y
apt-get install -y gcc python3-dev python3-pip default-libmysqlclient-dev libssl-dev git
```

### 3. 创建Python虚拟环境
```bash
pip3 install --upgrade pip virtualenv
python3 -m virtualenv venv
source venv/bin/activate
```

### 4. 安装Python依赖
```bash
pip install -r requirements.txt
```

### 5. 配置环境变量
```bash
cp .env.example .env
# 编辑.env文件，设置SECRET_KEY等参数
```

### 6. 初始化数据库
```bash
python -c "from app import create_app, db; from app.models import User; app = create_app('prod'); with app.app_context(): db.create_all(); if not User.query.filter_by(username='admin').first(): admin = User(username='admin', email='admin@example.com'); admin.set_password('admin123'); db.session.add(admin); db.session.commit(); print('默认管理员账号已创建')"
```

### 7. 运行应用

**开发环境:**
```bash
python run.py
```

**生产环境（使用gunicorn）:**
```bash
gunicorn -w 4 -b 0.0.0.0:8888 app:create_app('prod')
```

## 使用说明

### 首次访问
1. 安装完成后，访问 http://服务器IP:8888
2. 使用默认账号登录：
   - 用户名：admin
   - 密码：admin123

### 登录后的操作
1. **立即修改密码**：登录后请立即修改默认密码
2. **查看系统概览**：在仪表盘查看服务器实时状态
3. **添加用户**：根据需要添加其他用户账号
4. **配置监控**：设置监控参数和告警阈值

### 主要功能模块

#### 1. 仪表盘
- 系统资源使用概览
- CPU、内存、磁盘实时图表
- 系统负载和运行时间

#### 2. 服务器监控
- 详细的CPU使用率统计
- 内存使用情况分析
- 磁盘空间和IO监控
- 网络流量统计

#### 3. 进程管理
- 查看所有运行中的进程
- 按CPU/内存使用率排序
- 查看进程详细信息
- 结束进程（需谨慎操作）

#### 4. 文件管理
- 浏览服务器文件系统
- 上传和下载文件
- 在线编辑文本文件
- 创建、删除、移动文件和目录

#### 5. 网络管理
- 查看所有网络连接
- 监控网络流量
- 查看网络接口信息

#### 6. 系统日志
- 查看系统日志文件
- 搜索和过滤日志内容
- 监控实时日志

## 系统服务管理

### 启动服务
```bash
systemctl start tiny-panel
```

### 停止服务
```bash
systemctl stop tiny-panel
```

### 重启服务
```bash
systemctl restart tiny-panel
```

### 查看服务状态
```bash
systemctl status tiny-panel
```

### 查看日志
```bash
tail -f /var/log/tiny-panel/tiny-panel.log
```

## 配置文件说明

### .env文件
- `SECRET_KEY`：应用密钥，用于加密会话数据
- `DATABASE_URL`：数据库连接URL，默认使用SQLite
- `LOG_TO_STDOUT`：是否将日志输出到标准输出
- `FLASK_ENV`：Flask运行环境（development/production）

## 安全建议

1. **修改默认密码**：安装后立即修改admin账号密码
2. **定期更新**：定期更新应用代码和依赖
3. **限制访问IP**：使用防火墙限制只允许特定IP访问8888端口
4. **使用HTTPS**：在生产环境中配置HTTPS
5. **最小权限**：运行应用时使用最小权限的用户

## 故障排除

### 常见问题

1. **无法访问面板**
   - 检查服务器防火墙是否开放8888端口
   - 检查tiny-panel服务是否正常运行
   - 查看日志文件查找错误信息

2. **数据库连接错误**
   - 检查.env文件中的DATABASE_URL配置
   - 确保数据库服务正在运行

3. **服务启动失败**
   - 查看系统服务日志：`journalctl -u tiny-panel`
   - 检查Python依赖是否正确安装
   - 确认端口8888未被占用

## 更新方法

### 使用一键安装脚本更新
```bash
curl -sSL http://www.tiny-lab.cn/dl/install.sh | bash -s -- --update
```

### 手动更新
```bash
cd /opt/tiny-panel
git pull
source venv/bin/activate
pip install -r requirements.txt
systemctl restart tiny-panel
```

## 许可证
MIT License

## 贡献
欢迎提交Issue和Pull Request来帮助改进这个项目！

## 联系方式
- 项目地址：https://github.com/tiny-panel/tiny-panel
- 官方网站：http://www.tiny-lab.cn
- 支持邮箱：support@tiny-lab.cn

---

**注意**：本项目仅供学习和个人使用，在生产环境中使用时请自行承担风险。